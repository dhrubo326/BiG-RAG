# BiG-RAG Test Dependencies
#
# NOTE: This file is now OPTIONAL. The recommended way to install test dependencies is:
#   pip install -e ".[test]"
#
# This file is kept for backward compatibility and manual installations.
# Install with: pip install -r requirements-test.txt

# Core testing framework
pytest
pytest-asyncio
pytest-timeout
pytest-xdist        # Parallel test execution

# Coverage reporting
pytest-cov
coverage

# Mocking and fixtures
pytest-mock
faker                # Generate synthetic test data

# API testing
httpx                   # Async HTTP client for API tests
requests                # Sync HTTP client (fallback)

# Performance testing
pytest-benchmark
memory-profiler

# HTML reports (optional - can comment out in production)
pytest-html

# Frontend testing (optional - can comment out if no frontend)
# selenium
# playwright

# Data validation
pydantic>=2.0.0
 
# Utilities
python-dotenv           # Load test environment variables
colorama                # Colored output for Windows terminal
